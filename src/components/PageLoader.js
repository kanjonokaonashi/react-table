import React from "react";

const PageLoader = (props) => {
    return (
            <div className="loader">
                <h1>LOADING.....</h1>
            </div>
    )
}

export default PageLoader;